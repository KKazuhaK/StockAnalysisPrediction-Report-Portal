name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  go-test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: portal
          POSTGRES_PASSWORD: portal
          POSTGRES_DB: reports_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U portal -d reports_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      # Enables the Postgres integration test (TestPostgresQueries); without this the
      # test skips. Verifies the real PG path: schema DDL, STRING_AGG, ILIKE search.
      TEST_POSTGRES_DSN: "postgres://portal:portal@localhost:5432/reports_test?sslmode=disable"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - run: go build ./... # compile every package (fails fast on a build break)
      - run: go vet ./...
      # staticcheck: static analysis beyond go vet (real bugs, dead code, simplifications).
      # Pinned; the tree already passes it clean. See docs/adr/0016-app-package-architecture.md.
      - run: go run honnef.co/go/tools/cmd/staticcheck@2025.1.1 ./...
      # govulncheck: fails on a known vulnerability reachable from our code. A hard gate rather than
      # advisory because we own an XML signature verifier (SAML, ADR 0023): its signing dependencies
      # have shipped signature-bypass CVEs, and a routine `go mod tidy` must not be able to walk the
      # module graph back onto a vulnerable version without CI noticing.
      - run: go run golang.org/x/vuln/cmd/govulncheck@latest ./...
      - run: go test ./... -v

  web-build:
    name: web typecheck + build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: web/package-lock.json
      - name: Install deps
        working-directory: web
        run: npm ci
      - name: Typecheck # tsc --noEmit: catches type errors the build would tolerate
        working-directory: web
        run: npm run typecheck
      - name: Unit tests
        working-directory: web
        run: npm run test
      - name: Build SPA # vite build → ../internal/web/dist; fails on any compile error
        working-directory: web
        run: npm run build

  # The font chain used to be executed for the first time during a release: Dockerfile.release runs
  # only on a tag, so a typo in it surfaced after the binaries were built and, before the release job
  # learned to wait on this one, after the GitHub Release was already public. Two releases were lost
  # that way. `--target runtime-base` stops before the stage that copies the Go binary, so this needs
  # no build artifacts — it fetches the fonts, patches the CJK cmap and runs the coverage gate, which
  # is the part that actually breaks.
  font-image:
    name: font chain + coverage gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build the font stages and run the gate
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.release
          target: runtime-base
          platforms: linux/amd64 # one arch is enough to catch a Dockerfile or gate error
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
